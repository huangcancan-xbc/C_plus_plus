#pragma once
#include <iostream>
#include <assert.h>
#include <algorithm>
using namespace std;

// class Date
// {
// private:
//     int year, month, day;
//     static const int Days_per_month[13];
//     static bool is_valid_date(int y, int m, int d);             // 判断是否为合法日期
//     static bool is_leap_year(int year);                         // 判断是否为闰年
//     static int get_month(int year, int month);                  // 获取每月的天数
//     static bool compare_dates(const Date& d1, const Date& d2);  // 判断两个日期的“大小”
//     int to_days() const;                                        // 日期转为天数

// public:
//     Date(int y, int m, int d);                                  // 构造函数
//     void print() const;                                         // 打印日期
//     void add_days(int x);                                       // 日期加法
//     void sub_days(int x);                                       // 日期减法
//     int date_difference(const Date other) const;                // 日期差值
// };


void test()
{
    cout << "Hello, World!" << endl;
    cout << "Hello, World!" << endl;
    cout << "Hello, World!" << endl;
    cout << "Hello, World!" << endl;
    cout << "Hello, World!" << endl;
}